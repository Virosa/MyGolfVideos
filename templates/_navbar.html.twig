
<nav class="navbar">


        <div class="navbar-left">
            <a class="nav-link-brand" href="{{ path('app_home') }}">My Golf Videos</a>
        </div>
            <div class="navbar-right">
                {% if app.user %}

                    <span class="nav-link">{{ app.user.email }}</span>


                        <a class="nav-link" href="{{ path('app_category_index') }}">Catégories</a>

                        <a class="nav-link" href="{{ path('app_video') }}">Vidéos</a>

                        {#<a class="nav-link" href="{{ path('app_register') }}">Inscription</a>#}

                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                        {#<a class="nav-link" href="{{ path ('app_login') }}">Connexion</a>#}

                        {% else %}
                            <a class="nav-link" href="{{ path('app_category_index') }}">Catégories</a>

                            <a class="nav-link" href="{{ path('app_video') }}">Vidéos</a>

                            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>

                            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                        {% endif %}
            </div>

        </nav>


